{
  "version": 2,
  "builds": [
    {
      "src": "api/tasks/**/*.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/tasks/(.*)",
      "dest": "api/tasks/$1.ts"
    }
  ]
}
